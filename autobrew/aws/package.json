{
  "name": "autobrew-aws",
  "description": "",
  "version": "0.1.1",
  "dependencies": {
    "serverless-domain-manager": "^5.1.0"
  },
  "scripts": {
    "deploy-uat": "sls create_domain --stage uat --region eu-west-1 && serverless deploy --stage uat --region eu-west-1",
    "deploy-prod": "sls create_domain --stage prod --region eu-west-1 && serverless deploy --stage prod --region eu-west-1"
  },
  "devDependencies": {
    "serverless": "^2.29.0",
    "serverless-pseudo-parameters": "^2.5.0",
    "serverless-python-requirements": "^5.1.0"
  }
}
